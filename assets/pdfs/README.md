# /assets/pdfs/

PDF assets uploaded to the WP media library.
Filled by `scripts/download-pdfs.sh` from `media-inventory.csv` rows of type `pdf`.

Tracked in git so that filenames are stable across machines.
